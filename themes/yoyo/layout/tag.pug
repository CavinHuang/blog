extends includes/layout.pug

block content
  include ./includes/mixins/article_sort.pug
  .archive(style="overflow: hidden;")
    .article-sort-title= page.tag + page.posts.length
    +articleSort(page.posts)
  include includes/pagination.pug
